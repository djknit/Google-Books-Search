import React from 'react';
import './style.css';

export default ({
  height
}) => {
  
  return(
    <footer id="my-footer" className="has-shadow is-deep is-low" style={{ height }}>
    </footer>
  );
}